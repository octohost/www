box: wercker/nodejs
build:
  steps:
    - npm-install
deploy:
  steps:
    - add-to-known_hosts:
        hostname: server.octohost.io
    - add-ssh-key:
        keyname: KEY
    - script:
        name: Push to dokku
        code: |
          git push $GIT_REMOTE master
