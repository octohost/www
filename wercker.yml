box: wercker/nodejs
deploy:
  steps:
    - add-to-known_hosts:
        hostname: server.octohost.io
    - add-ssh-key:
        keyname: KEY
    - script:
        name: Push to octohost
        code: |
          git push $GIT_REMOTE master
