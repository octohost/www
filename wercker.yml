box: wercker/nodejs
# Build definition
build:
  # The steps that will be executed on build
  steps:
    - script:
        name: harp compile
        code: |
          sudo npm -g install
          harp compile
deploy :
  steps :
    - script:
        name: Deploy to octohost.
        code: |-
          git push $GIT_REMOTE master
